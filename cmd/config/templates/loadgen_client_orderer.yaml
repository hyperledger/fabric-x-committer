# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
# This is a partial template. It contains only the orderer client configurations.
# It should be complimented by the common load generator configuration.

orderer-client:
  sidecar-client:
    endpoints:
      - {{ .Endpoints.Sidecar.Server }}
    tls:
      mode: {{ .ClientCreds.Mode }}
      cert-path: {{ .ClientCreds.CertPath }}
      key-path: {{ .ClientCreds.KeyPath }}
      ca-cert-paths:
        {{- range .ClientCreds.CACertPaths }}
        - {{ . }}
        {{- end }}
  orderer:
    connection:
      endpoints:
        {{- range .Endpoints.Orderer }}
        - {{ .Server }}
        {{- end }}
    consensus-type: BFT
    channel-id: {{ .ChannelID }}
  broadcast-parallelism: 5
