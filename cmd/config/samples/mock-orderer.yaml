# Copyright IBM Corp. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
---
server:
  endpoint: :7050
  tls:
    # TLS configuration being built based on the tls mode chosen: none, tls, mtls.
    mode: mtls
    cert-path: /orderer-certs/public-key.pem
    key-path: /orderer-certs/private-key.pem
    ca-cert-paths:
      - /orderer-certs/ca-certificate.pem

block-size: 1024
block-timeout: 30s
send-genesis-block: true
artifacts-path: /root/artifacts

logging:
  logSpec: "info"
  format: >-
    %{color}%{time:2006-01-02 15:04:05.000 MST} [%{module}] %{shortfunc}
    -> %{level:.4s}%{color:reset} %{message}

